//
//  Util.cpp
//
//  Created by Egaro555 on 30/01/2018.
//
//

#include "Util.h"

// PRIVATE ============================================================ PRIVATE
// ------------------------------------------------------------------- methodes

// END PRIVATE
// PUBLIC ============================================================== PUBLIC

// ------------------------------------------------------------------- Methodes
/*static vector<LedsGroup> Util::genGroupvector(byte** pinsLedsRGB, byte* pinGroups){
  vector<LedsGroup> res;
  ushort nbLeds = (sizeof(pinsLedsRGB)/(sizeof(*pinsLedsRGB)));
  ushort nbGroup = (sizeof(pinGroups)/sizeof(*pinGroups));

  for(ushort iG = 0 ; iG<nbGroup ; iG++){
    LedGroup* = new LedsGroup(pinGroup[])
    for(ushort iL = 0 ; iL<nbLeds ; iL++){

    }
  }

  res.addLed(new LedRGB())
}*/
// END PUBLIC
// PROTECTED ======================================================== PROTECTED

// END PUBLIC
